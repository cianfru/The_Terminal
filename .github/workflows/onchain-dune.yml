name: Daily on-chain refresh (ETH Dune + Solana RPC + Base Alchemy)

# Hands-off weekly refresh with NO BigQuery / NO GCP — uses the Dune credits the account
# already has. The full raw-transfer history lives in a GitHub RELEASE ASSET (transfers.csv.gz);
# each run downloads it, pulls only the WEEKLY DELTA from Dune (cheap: ~17k rows, no 402),
# merges, re-runs the LOCAL FIFO engine → onchain.json + urpd.json, then re-uploads the grown
# archive. SEED ONCE before the first run (see below).
#
# CREDITS: only the WEEKLY DELTA touches Dune (~a few credits/run, ~tens/month — nowhere near the
# 2,500 budget). The SEED (full history) is a ONE-TIME step and does NOT run here automatically.
#
# 🔲 ONE-TIME SEED (owner) — pick the ZERO-CREDIT path:
#    • PREFERRED (0 Dune credits): re-run the FREE BigQuery extract (bigquery/spx6900_raw_transfers.sql,
#      546 GB inside BigQuery's free 1 TB/mo — NOT Dune) — or reuse the CSV you already have — then:
#        gzip -c full_transfers.csv > transfers.csv.gz
#        gh release create onchain-archive transfers.csv.gz -t "On-chain transfer archive" -n "FIFO base"
#    • ALT (Dune, ~6 credits one-time, watch the meter): run locally
#        node scripts/build-onchain-dune-refresh.mjs --archive=transfers.csv --seed
#      then gzip + `gh release create onchain-archive transfers.csv.gz …`.
#    This workflow FAILS fast if the asset is missing — it will NOT auto-seed / spend credits.
# 🔲 SECRETS/VARS: DUNE_API_KEY (secret). Optional DUNE_INCREMENTAL_QUERY_ID (var) — set after the
#    first run logs the created query id, to reuse it instead of creating a new one each week.

on:
  # PRIMARY daily on-chain refresh + SINGLE SOURCE OF TRUTH (owner, 2026-07-27). The full transfer
  # history lives as the GitHub release asset (transfers.csv.gz); each run pulls ONLY the incremental
  # Dune delta (WHERE evt_block_time >= last day), merges, and runs the local FIFO engine at DAILY
  # granularity (--daily, set in build-onchain-dune-refresh.mjs). Dune's "result read" bills by rows,
  # not runs, so daily ≈ weekly ≈ ~150-180 credits/mo (~7% of the 2,500 budget). BigQuery
  # (onchain-bigquery.yml) is dispatch-ONLY now — one-time reconstructions + emergency backup — so
  # the two can never both commit onchain.json.
  # THE SINGLE daily on-chain cron. ETH (Dune incremental → local FIFO) is the primary job; Solana
  # (keyless public RPC snapshot-forward) and Base (Alchemy ≥5k cohort delta) are folded in as
  # continue-on-error steps below, so all three chains refresh in ONE run / ONE commit / ONE deploy
  # instead of three staggered crons. A Solana or Base hiccup can never block the ETH commit + deploy.
  schedule:
    - cron: "23 5 * * *"   # ~05:23 UTC daily
  workflow_dispatch:
    inputs:
      full:
        description: "Base: force a full cohort rebuild from genesis (self-heal)"
        type: boolean
        default: false

permissions:
  contents: write           # release download/upload + commit

concurrency:
  group: onchain-dune
  cancel-in-progress: false

env:
  ARCHIVE_TAG: onchain-archive
  ARCHIVE_ASSET: transfers.csv.gz
  # BigQuery ETH source (used once GCP_SA_KEY is set; until then this workflow stays on Dune).
  GCP_PROJECT: goog-fltx
  BQ_DATASET: spx_onchain
  BQ_TABLE: eth_transfers
  SPX_TOKEN: "0xe0f63a424a4439cbe457d80e4f4b51ad25b2c56c"
  BQ_MAX_BYTES: "214748364800"   # 200 GB cap on the incremental append — the delta is a few GB; a full
                                 # backfill (empty table) exceeds this and fails cleanly, forcing the one-time seed.

jobs:
  refresh:
    runs-on: ubuntu-latest
    outputs:
      deploy: ${{ steps.commit.outputs.deploy }}
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
      # Most builders use only Node built-ins, but the card-render steps (e.g. build-whale-thennow →
      # @resvg/resvg-js) need the npm deps installed — without this they failed silently under `|| true`.
      - run: npm ci

      # ── ETH SOURCE: BigQuery once GCP_SA_KEY is set, else Dune ──────────────────────────────────
      # Dune is going view-only (free plan, 2026-09-10). BigQuery (public crypto_ethereum, its own
      # append table, FREE) is the permanent replacement. This gate keeps the workflow on Dune until
      # you add the GCP_SA_KEY secret — the moment it's set, ETH flips to BigQuery with NO other change
      # (Solana RPC + Base Alchemy + the entire downstream, incl. SPX City, are untouched either way).
      - name: Pick ETH source (BigQuery if GCP_SA_KEY set, else Dune)
        id: src
        env:
          GCP_SA_KEY: ${{ secrets.GCP_SA_KEY }}
        run: |
          if [ -n "$GCP_SA_KEY" ]; then echo "mode=bq"   >> "$GITHUB_OUTPUT"; echo "ETH source → BigQuery";
          else                         echo "mode=dune" >> "$GITHUB_OUTPUT"; echo "ETH source → Dune (add GCP_SA_KEY to switch to BigQuery)"; fi

      # ── DUNE path (default until GCP_SA_KEY exists) ──
      - name: Download the transfer archive (release asset)
        if: steps.src.outputs.mode == 'dune'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if ! gh release download "$ARCHIVE_TAG" --pattern "$ARCHIVE_ASSET" --dir . 2>/dev/null; then
            echo "::error::No '$ARCHIVE_ASSET' on release '$ARCHIVE_TAG'. SEED it first — upload the full 2.6M-row extract (see the workflow header)."
            exit 1
          fi
          gunzip -f "$ARCHIVE_ASSET"
          echo "archive rows: $(( $(wc -l < transfers.csv) - 1 ))"

      - name: Pull Dune delta → merge → run FIFO engine
        if: steps.src.outputs.mode == 'dune'
        env:
          DUNE_API_KEY: ${{ secrets.DUNE_API_KEY }}
          DUNE_INCREMENTAL_QUERY_ID: ${{ vars.DUNE_INCREMENTAL_QUERY_ID }}
        run: node scripts/build-onchain-dune-refresh.mjs --archive=transfers.csv

      # ── BIGQUERY path (once GCP_SA_KEY is set): own append table = the archive; export → FIFO ──
      - name: BigQuery — auth
        if: steps.src.outputs.mode == 'bq'
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}
      - name: BigQuery — setup gcloud
        if: steps.src.outputs.mode == 'bq'
        uses: google-github-actions/setup-gcloud@v2
      - name: BigQuery — incremental append + export → transfers.csv
        if: steps.src.outputs.mode == 'bq'
        # First run: the table is SEEDED once via `bq load` (see the header) so it's never empty here;
        # the append then partition-prunes on block_timestamp → only new days scanned (a few GB). The
        # 200 GB cap fails a stray full-scan cleanly rather than eating the free 1 TB/mo.
        #
        # ⚠ PARTITION PRUNING NEEDS A LITERAL BOUND. crypto_ethereum.token_transfers is day-partitioned
        # on block_timestamp, but the planner only prunes when the bound is a CONSTANT. A subquery bound
        # ("block_timestamp > (SELECT MAX(time) …)") is opaque at plan time, so BigQuery scanned the WHOLE
        # ~605 GB history and blew the 200 GB cap. Fix: read MAX(time) FIRST (tiny scan of our own table),
        # then inject it as a LITERAL into the incremental INSERT so pruning fires (≈ a day's partition, a
        # few GB). Strict `>` (never `>=`) so a re-run can't duplicate the boundary second into the table.
        run: |
          bq --project_id="$GCP_PROJECT" mk --dataset --location=US "$GCP_PROJECT:$BQ_DATASET" 2>/dev/null || true
          bq --project_id="$GCP_PROJECT" query --use_legacy_sql=false --format=none \
            "CREATE TABLE IF NOT EXISTS \`$GCP_PROJECT.$BQ_DATASET.$BQ_TABLE\` (sender STRING, receiver STRING, time TIMESTAMP, value BIGNUMERIC)"

          # cutoff = the newest time already in our table, as a bare literal string (strip CSV header + quotes)
          CUTOFF=$(bq --project_id="$GCP_PROJECT" query --use_legacy_sql=false --format=csv \
            "SELECT FORMAT_TIMESTAMP('%Y-%m-%d %H:%M:%E6S', COALESCE(MAX(time), TIMESTAMP '2000-01-01 00:00:00')) FROM \`$GCP_PROJECT.$BQ_DATASET.$BQ_TABLE\`" \
            | tail -n1 | tr -d '"')
          echo "incremental cutoff (newest archived time): $CUTOFF"
          if [ -z "$CUTOFF" ]; then echo "::error::could not read MAX(time) — is the table seeded?"; exit 1; fi

          bq --project_id="$GCP_PROJECT" query --use_legacy_sql=false --format=none --maximum_bytes_billed="$BQ_MAX_BYTES" \
            "INSERT INTO \`$GCP_PROJECT.$BQ_DATASET.$BQ_TABLE\` (sender, receiver, time, value)
             SELECT from_address, to_address, block_timestamp, CAST(value AS BIGNUMERIC)
             FROM \`bigquery-public-data.crypto_ethereum.token_transfers\`
             WHERE block_timestamp > TIMESTAMP '$CUTOFF'
               AND token_address = '$SPX_TOKEN'"

          bq --project_id="$GCP_PROJECT" query --use_legacy_sql=false --format=csv --max_rows=5000000 \
            "SELECT sender, receiver, time, value FROM \`$GCP_PROJECT.$BQ_DATASET.$BQ_TABLE\` ORDER BY time" > transfers.csv
          echo "BigQuery transfers rows: $(( $(wc -l < transfers.csv) - 1 ))"
      # ── FRESHNESS: fill the public-dataset's ~1–2 day lag from a LIVE node (Alchemy) ──
      # The BigQuery export above only reaches ~2 days ago (Google's crypto_ethereum ETL is batch).
      # This appends SPX transfers from that edge to the latest block via alchemy_getAssetTransfers
      # (the same live source Zerion/Etherscan read — we already use it for Base + AEON), so onchain /
      # whales / exit-flow / smart-money / city all reconstruct through TODAY. The tail is appended to
      # transfers.csv ONLY, never to the BigQuery table — tomorrow the public dataset covers the same
      # days from canonical data, so there's no duplication. Soft (continue-on-error): a bad pull just
      # leaves the feeds at the ~2-day BigQuery edge, exactly as before.
      - name: "ETH — Alchemy leading-edge tail (freshness: BigQuery edge → now)"
        if: steps.src.outputs.mode == 'bq'
        continue-on-error: true
        env: { ALCHEMY_KEY: "${{ secrets.ALCHEMY_KEY }}" }
        run: node scripts/build-eth-transfers-alchemy-tail.mjs --archive=transfers.csv
      - name: BigQuery — run FIFO engine (no Dune pull)
        if: steps.src.outputs.mode == 'bq'
        run: node scripts/build-onchain-dune-refresh.mjs --archive=transfers.csv --no-pull
      - name: "Classify self-move + entity addresses (contract vs EOA — entity foundation)"
        # eth_getCode over the addresses in the self-moves.json AND entities.json the FIFO run just wrote →
        # grows the public/addr-types.json cache. The NEXT FIFO run reads it to withhold re-aging from moves
        # that touch a contract/Safe AND to keep the entity graph from linking wallets THROUGH an untagged
        # Safe/router. Soft by design (exits 0 on any RPC hiccup) so it can't block the commit.
        continue-on-error: true
        env: { ETH_RPC: "${{ vars.ETH_RPC }}" }
        run: node scripts/enrich-addr-types.mjs --from=public/self-moves.json,public/entities.json --out=public/addr-types.json
      - name: Rebuild the city time-machine (weekly per-wallet balances)
        # rides the same merged archive — no extra pull; feeds the launch→today slider.
        # continue-on-error: a secondary transform must never block committing the core onchain.json.
        continue-on-error: true
        run: node scripts/build-city-timeline.mjs --spx=transfers.csv --out=public/spx-timeline.json
      - name: "Rebuild the city recap (weekly/monthly — new residents, tier upgrades)"
        # pure diff of the timeline just built — no data pull; feeds the City report card + panel
        continue-on-error: true
        run: node scripts/build-city-recap.mjs --timeline=public/spx-timeline.json --out=public/city-recap.json
      - name: Rebuild cohort survival (who still holds, by arrival era)
        # continue-on-error: secondary re-slice of the timeline — can't be allowed to block onchain.json
        continue-on-error: true
        run: node scripts/build-cohort-survival.mjs --in=public/spx-timeline.json --out=public/cohort-survival.json
      - name: Rebuild "Whales Then vs Now" panels (mosaic at the NRPL-picked cycle moments)
        # Small precompute off the timeline + onchain NRPL, so the card/og read a 50 KB file not the 3 MB timeline.
        continue-on-error: true
        run: node scripts/build-whale-thennow.mjs
      - name: Rebuild cohort ROI (lifetime % in profit + typical multiple by whale size band)
        # Off the timeline + price-history — lifetime average-cost P&L per wallet, scored by size band.
        continue-on-error: true
        run: node scripts/build-cohort-roi.mjs
      - name: Rebuild whale entries (when each ≥100k whale bought → the bubble card/chart)
        continue-on-error: true
        run: node scripts/build-whale-entry.mjs
      - name: Rebuild whale-cohort history (WEEKLY counts in each size cohort)
        # From the lightweight spx-timeline.json (built above), NOT the 2.6M-row archive. The archive
        # path OOM'd (it loaded + sorted every transfer), so it silently failed under continue-on-error
        # and left "Whale Counts Over Time" frozen at a stale count while whales.json moved on — the
        # 644-vs-1026 mismatch. The timeline path is memory-safe AND reconciles EXACTLY to whales.json
        # (same current-membership counts, same bands), so the chart and the watcher can't drift.
        continue-on-error: true
        run: node scripts/build-whale-cohorts.mjs --timeline=public/spx-timeline.json --out=public/whale-cohort-history.json
      - name: Rebuild exit flow (DAILY departures split by profit/loss — who's selling, at what price)
        # straight off the raw archive → exact per-day exit, finer than the weekly timeline
        # continue-on-error: a failure here must never block the core onchain.json commit (weekly seed persists)
        continue-on-error: true
        run: node scripts/build-exit-flow.mjs --transfers=transfers.csv --prices=public/price-history.json --out=public/exit-flow.json
      - name: Rebuild smart-money cohort (DAILY, proven top-timers, aggregate + new qualifiers)
        # off the raw archive → real per-transfer timestamps, daily net-flow (finer than the weekly timeline)
        # continue-on-error + a raised heap (loads+sorts the full ~2.6M-row archive) so it can't OOM-block the pipeline
        continue-on-error: true
        env:
          NODE_OPTIONS: --max-old-space-size=6144
        run: node scripts/build-smart-money.mjs --transfers=transfers.csv --prices=public/price-history.json --out=public/smart-money.json
      - name: Rebuild city history (DAILY citizens + TVL by cohort — how the city grew)
        # off the raw archive → real per-transfer timestamps, daily residency (≥5k held 90d) + TVL.
        # continue-on-error + raised heap (loads+sorts the full archive) so it can't OOM-block the pipeline.
        continue-on-error: true
        env:
          NODE_OPTIONS: --max-old-space-size=6144
        run: node scripts/build-city-history.mjs --transfers=transfers.csv --prices=public/price-history.json --out=public/city-history.json
      - name: Reconcile the city land registry (deeds — which wallet holds which lot)
        # ⭐ MUST RUN AFTER whales.json IS REBUILT. The registry reconciles against TODAY's residents:
        # a wallet that fell below the bar goes vacant (its lot held 30 days), one that returns gets the
        # same lot back, one that crossed a tier boundary is rehoused. Reading a stale whales.json would
        # mark live residents as departed.
        #
        # Cheap and local — no archive, no network, ~1s on the committed JSON, so no heap bump needed.
        # continue-on-error because a deed hiccup must never cost the day's on-chain commit + deploy:
        # the registry is idempotent, so tomorrow's run reconciles from the same file and catches up.
        # On the FIRST run public/city-deeds.json does not exist and the script SEEDS it from placeCity's
        # own output, which freezes the layout the city already draws — nothing moves on the day it ships.
        continue-on-error: true
        run: node scripts/build-city-deeds.mjs

      - name: Sanity check (reconcile to the known extract)
        run: |
          node -e '
            const o=JSON.parse(require("fs").readFileSync("public/onchain.json","utf8"));
            const last=o.at(-1); console.log("latest:",JSON.stringify(last));
            if(!(last.holders>40000&&last.holders<60000)){console.error("holders off — expected ~49.5k");process.exit(1);}
            if(!(last.rp>0.3&&last.rp<0.8)){console.error("realized price off — expected ~0.53");process.exit(1);}
            console.log("reconciles: rp",last.rp,"holders",last.holders,"top100",last.top100);
          '

      - name: Re-upload the grown archive
        # DUNE path only — in BigQuery mode the append table IS the archive (nothing to upload).
        # continue-on-error: the ETH data is already computed + sanity-checked, so a transient GitHub
        # upload hiccup must not cost the day's commit + deploy. The merge is idempotent — if the archive
        # doesn't grow today, the next run re-pulls the same delta from the same cutoff and recomputes.
        if: steps.src.outputs.mode == 'dune'
        continue-on-error: true
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gzip -f transfers.csv
          gh release upload "$ARCHIVE_TAG" "$ARCHIVE_ASSET" --clobber

      # ── Solana (folded in from the retired solana-onchain.yml) ─────────────────────────────────
      # Keyless public-RPC snapshot-forward: one getProgramAccounts call snapshots every SPX token
      # account, sums by owner, keeps the ≥5k cohort, appends to the committed archive, and rebuilds
      # public/solana-onchain.json. continue-on-error so a Solana hiccup never blocks the ETH commit.
      - name: Solana — public-RPC snapshot → merge → rebuild residents
        continue-on-error: true
        env:
          SOL_RPC: ${{ secrets.SOL_RPC }}   # optional free Helius/QuickNode URL; blank → public mainnet-beta
        run: |
          if [[ -s dune/out/spx6900_solana_balances.csv ]]; then
            node scripts/build-solana-rpc-snapshot.mjs --archive=dune/out/spx6900_solana_balances.csv
          else
            echo "::warning::Solana archive missing — skipping Solana refresh"
          fi

      # ── Base (folded in from the retired base-onchain.yml) ─────────────────────────────────────
      # Alchemy ≥5k cohort delta: pulls transfers since lastBlock, replays onto per-wallet balances,
      # updates the cohort (crossings/exits), rebuilds public/base-onchain.json. continue-on-error.
      - name: Base — Alchemy delta → replay → rebuild base-onchain.json
        continue-on-error: true
        env:
          ALCHEMY_KEY: ${{ secrets.ALCHEMY_KEY }}
          NODE_OPTIONS: --max-old-space-size=4096
        run: node scripts/build-base-alchemy.mjs ${{ (github.event.inputs.full == 'true') && '--full' || '' }}

      - name: Rebuild the Brief + daily snapshot on the FRESH on-chain feeds
        # These digests are ALSO built in the 00:17 snapshot cron — but that runs BEFORE this on-chain
        # refresh (05:23), so there they read YESTERDAY's whale / smart-money / onchain pull. Rebuild
        # them here now that onchain/whales/entities/self-moves/smart-money are today's data, or the
        # Terminal + Brief would permanently lag a day on every whale read. Keyless, pure transform.
        continue-on-error: true
        run: |
          node scripts/bot/notable.mjs || true
          node scripts/bot/daily-snapshot.mjs || true

      # THE DATA WALL: entities.json (real cluster addresses) + spx-timeline.json (per-wallet balances
      # that identify holders) are NOT committed to the public repo. They are built locally above (so the
      # downstream steps that read them still work), pushed to the private store (KV), and served only to
      # logged-in members via /api/auth?action=data. Needs KV_REST_API_URL/TOKEN as Actions secrets; if
      # unset this soft-skips (members then see no data until the secrets are added + this reruns).
      - name: Push members-only feeds to the private store (KV)
        env:
          KV_REST_API_URL: ${{ secrets.KV_REST_API_URL }}
          KV_REST_API_TOKEN: ${{ secrets.KV_REST_API_TOKEN }}
        run: |
          # Fully-private feed (no public copy at all): entities.json (the proprietary wallet-cluster
          # LINKAGE — who-owns-what — powering the members-only Wallet Clusters + Cluster City).
          node scripts/push-private-feed.mjs --file=public/entities.json --key=entities || true
          # Dual-use members feeds: push the FULL (granular, addresses/lots) version to KV for members,
          # THEN strip the identifying fields from the committed public copy so the X cards + aggregate
          # views stay public but no address list is. Order matters: push full first, strip second.
          node scripts/push-private-feed.mjs --file=public/smart-money.json --key=smart-money || true
          node scripts/push-private-feed.mjs --file=public/whale-entry.json --key=whale-entry || true
          node scripts/push-private-feed.mjs --file=public/whale-lots.json --key=whale-lots || true
          node scripts/strip-private-fields.mjs --file=public/smart-money.json --drop-key=wallets || true
          node scripts/strip-private-fields.mjs --file=public/whale-entry.json --drop-field=whales:a || true
          node scripts/strip-private-fields.mjs --file=public/whale-lots.json --drop-key=wallets || true
          # whales.json + spx-timeline.json are PUBLIC (SPX City is a public showpiece): committed in full
          # with addresses, NOT pushed to KV or stripped.

      - name: Commit if changed
        id: commit
        run: |
          # ETH/site files drive the DEPLOY (the site + og cards read these). Solana/Base files are
          # bot-only (no site consumer), so they ride along in the same commit but don't force a deploy
          # on their own. COMMIT if ANY of them changed; DEPLOY only if an ETH/site file changed.
          # entities.json is DELIBERATELY absent — it goes to KV (the data wall) only, not the public repo.
          # addr-types is derived from entities but carries no balances, so it stays. spx-timeline + whales
          # ARE committed (public — SPX City is a public showpiece).
          ETH_FILES="public/onchain.json public/urpd.json public/urpd-history.json public/whales.json public/spx-timeline.json public/self-moves.json public/addr-types.json public/cohort-survival.json public/exit-flow.json public/smart-money.json public/city-recap.json public/whale-cohort-history.json public/city-history.json public/cex-sankey.json public/whale-thennow.json public/cohort-roi.json public/whale-entry.json public/whale-lots.json public/city-deeds.json"
          CHAIN_FILES="public/solana-onchain.json dune/out/spx6900_solana_balances.csv public/base-onchain.json dune/out/spx6900_base_cohort.csv"
          # The control-panel/terminal digests: deploy-ignored (read via the raw proxy), so they commit
          # but never force a deploy — same as their 00:17 build.
          DIGEST_FILES="public/notable.json public/daily-snapshot.json"
          DEPLOY_CHANGED=""
          [[ -n "$(git status --porcelain $ETH_FILES)" ]] && DEPLOY_CHANGED=1
          if [[ -n "$(git status --porcelain $ETH_FILES $CHAIN_FILES $DIGEST_FILES)" ]]; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add $ETH_FILES $CHAIN_FILES $DIGEST_FILES
            git commit -m "chore: on-chain refresh — ETH/Solana/Base $(date -u +%F)"
            # This job takes ~2-3 min; a human (or another cron) can push to main inside that window,
            # which used to reject our push with a non-fast-forward and fail the whole daily refresh.
            # We only touch GENERATED files (rebuilt fresh every run), so on a conflict OUR just-built
            # version must win — `-X theirs` (during a rebase, "theirs" = the commit being replayed, i.e.
            # ours) auto-resolves instead of erroring out with a merge conflict. Retried against a busy branch.
            for i in 1 2 3 4 5; do
              git push && break
              echo "push rejected (attempt $i) — rebasing on the latest main and retrying"
              git pull --rebase -X theirs --autostash origin main || exit 1
              sleep $((i * 3))
            done
          else
            echo "No change to commit"
          fi
          [[ -n "$DEPLOY_CHANGED" ]] && echo "deploy=true" >> "$GITHUB_OUTPUT" || echo "deploy=false" >> "$GITHUB_OUTPUT"

  deploy:
    needs: refresh
    if: needs.refresh.outputs.deploy == 'true'
    runs-on: ubuntu-latest
    concurrency:
      group: vercel-production
      cancel-in-progress: true
    env:
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
    steps:
      - uses: actions/checkout@v5
        with:
          ref: main
      - uses: actions/setup-node@v5
        with:
          node-version: 20
      - run: npm install --global vercel@latest
      - run: vercel pull --yes --environment=production --token="$VERCEL_TOKEN"
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      - run: vercel build --prod --token="$VERCEL_TOKEN"
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      - run: vercel deploy --prebuilt --prod --token="$VERCEL_TOKEN"
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
