# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# X bot dry-run artifact
bot-preview*.png
bot-preview*.mp4

# Recap previews are reviewed live on the control panel (/control → Monthly
# recap), never committed. --dry-run writes these throwaway PNGs locally.
recap-preview-*.png
public/recap/

# local card preview renders (keep dune/out/*.csv as provenance)
dune/out/*.png

# Throwaway Playwright/render probes written to the repo root while debugging the 3D city.
# They are scratch, never part of the build, and one slipped into a commit once already.
v.mjs
dbg*.mjs
meas.mjs
chk*.mjs
check*.mjs
lab.mjs
aeon-sweep-preview.png
# Root-anchored: the rendered whale/city videos land in repo-root out/. Must NOT be a bare
# `out/` — that also matches dune/out/, which holds tracked provenance CSVs (see line 36), and
# broke the daily on-chain refresh's `git add` of a fresh dune/out/*.csv on CI.
/out/

# The data wall: entities.json (the proprietary wallet-cluster linkage) is served members-only from KV
# (api/auth?action=data), never committed to the public repo. (whales.json + spx-timeline.json are
# PUBLIC — SPX City is a public showpiece — so they are NOT ignored.)
public/entities.json

# ── INCIDENT RECORD — NEVER COMMITTED ──
# tools/incident/ holds the working file for a platform appeal: third-party handles,
# screenshots and timestamps. This repo is PUBLIC. Nothing in it may be published.
tools/incident/

# NFT identification index: a ~395 MB npz of ORB descriptors rebuilt from public
# metadata in seconds, plus Python bytecode. Regenerate, never commit.
tools/nft-id/out/
__pycache__/
*.pyc
