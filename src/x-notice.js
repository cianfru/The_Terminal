// Shared by the X-suspension popup (XNotice.jsx) and the posts page (PostsPage.jsx).
// A separate module so the eager popup doesn't pull the lazy posts page into the main bundle.
export const SUSPENDED_IMG = "/suspended.jpg";
export const COMMS_HANDLE = "lanternlabsmain";
